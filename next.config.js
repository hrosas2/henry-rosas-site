/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  compress: true,
  // If you deploy to a subpath, set basePath here.
  // basePath: "",
};

module.exports = nextConfig;
